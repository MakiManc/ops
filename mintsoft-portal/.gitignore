# Discovery dumps contain live warehouse data and customer addresses.
# They must never be committed. Phase 0 writes here.
discovery/

node_modules/
dist/
.wrangler/

# Local secrets. Never commit. Real values live as Cloudflare secrets.
.dev.vars
.env
.env.local
